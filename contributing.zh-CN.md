# 贡献指南

[English](contributing.md) | **简体中文**

欢迎通过 [Issue](https://github.com/mansucache/awesome-markdown/issues/new/choose) 推荐工具、纠正信息或分享可复现的使用体验，可以使用中文或英文。开发者可以推荐自己的作品，请说明与项目的关系。

## 收录范围

覆盖 Markdown 的写作、知识管理、开发、协作、格式转换与内容发布，包括应用、开发库、扩展、服务和规范。条目需要有明确的 Markdown 使用场景、官方链接，以及与已有工具的区别。Star 数不是收录门槛。

描述工具能做什么、适合什么任务，并注明影响选择的限制。区分原生 Markdown 文件、输入快捷语法、导入、导出和渲染；不能因为存在公开仓库，就把整个产品标为开源。价格、平台支持与兼容性等判断应有官方来源，不使用未经证实的效果宣传。

## 提交修改

1. 使用 Node.js 22 或更新版本，运行 `npm ci --ignore-scripts`。
2. 编辑 `data/catalog.json`，保留已有 ID。提供中英文名称和描述、分类、适用标签及带日期的核验依据。可参照相似条目；标签定义位于 `scripts/validate.mjs`。
3. 运行 `npm run build`，生成双语首页、旧英文兼容入口、核验与历史记录。
4. 运行 `npm run verify`，在 PR 中说明修改理由并附来源。

暂时无法补全双语内容时，可以先用 Issue 提交已有信息，由维护者协助补齐。不要只改生成文件，下次构建会覆盖它。

## 核验与纠错

`metadata` 表示基础来源检查；`source-review` 表示按备注核对了特定能力；`tested` 必须有版本、步骤与结果，链接可访问不等于实测。证据不足的条目使用 `needs-review` 核验级别与 `pending` 状态。退出推荐的条目保留原因与来源，便于复查。

分享体验时，请附工具版本、环境、最小输入、预期结果与实际结果，示例中去掉个人信息。维护方法见[维护说明](docs/maintenance.md)，现有依据见[核验记录](docs/review-log.zh-CN.md)。

对本清单的贡献采用 [CC0-1.0](LICENSE)，收录工具遵循各自许可证。
